type Wallet {
  privateKey: String!
  publicKey: String!
  mnemonic: String!
  address: String!
}

type Mutation {
  createWallet(network: String): Wallet!
}
type Query {
  getWalletDetails(address: String): Wallet
}
